# Dependencias del proyecto SOB4ES
# Entornos para Notebooks: Random Forest, Regresion (Ridge), XGBoost, RegressorChain y MLP (PyTorch)
# Revisado: verificadas contra los imports reales de los 11 notebooks del proyecto.

# Manipulacion y procesamiento de datos
pandas>=2.0.0
numpy>=1.24.0

# Modelado predictivo y metricas de evaluacion
scikit-learn>=1.3.0
xgboost>=2.0.0
torch>=2.1.0

# Almacenamiento y persistencia de modelos (.pkl)
joblib>=1.3.0

# Explicabilidad del modelo e interpretabilidad
shap>=0.42.0

# Visualizacion de graficos
# matplotlib: graficos base (barras, scatter, radar, matrices de confusion) en todos los notebooks.
# seaborn: heatmaps de matrices de confusion y barplots de importancia
#          (mlp_multisalida, xgb_multisalida, comparador_clasificacion).
matplotlib>=3.7.0
seaborn>=0.12.0

# Soporte para la ejecucion de Jupyter Notebooks
jupyter # la mas reciente a secas para que no salgan los warning sobre tqdm
jupyterlab>=4.0.0
notebook>=7.0.0